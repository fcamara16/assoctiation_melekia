<form method="post" action="{{ path('app_devenir_membre_delete', {'id': devenir_membre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ devenir_membre.id) }}">
    <button class="btn">Delete</button>
</form>
