{% extends 'base.html.twig' %}

{% block title %}DevenirMembre index{% endblock %}

{% block body %}
    <h1>DevenirMembre index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Email</th>
                <th>Telephone</th>
                <th>Presentation</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for devenir_membre in devenir_membres %}
            <tr>
                <td>{{ devenir_membre.id }}</td>
                <td>{{ devenir_membre.nom }}</td>
                <td>{{ devenir_membre.prenom }}</td>
                <td>{{ devenir_membre.email }}</td>
                <td>{{ devenir_membre.telephone }}</td>
                <td>{{ devenir_membre.presentation }}</td>
                <td>
                    <a href="{{ path('app_devenir_membre_show', {'id': devenir_membre.id}) }}">show</a>
                    <a href="{{ path('app_devenir_membre_edit', {'id': devenir_membre.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_devenir_membre_new') }}">Create new</a>
{% endblock %}
