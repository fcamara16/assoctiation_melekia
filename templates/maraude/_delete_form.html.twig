<form method="post" action="{{ path('app_maraude_delete', {'id': maraude.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maraude.id) }}">
    <button class="btn">Delete</button>
</form>
